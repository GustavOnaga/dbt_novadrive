name: 'dbt_novadrive'
version: '1.0.0'

profile: 'dbt_novadrive'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


seeds:
  dbt_novadrive:
    +schema: raw_novadrive

models:
  dbt_novadrive:

    staging:
      +schema: "{{ env_var('DBT_ENV') }}_stg_novadrive"  # O dbt usará como base 'stg_novadriva'
      +materialized: view

    intermediate:
          +schema: "{{ env_var('DBT_ENV') }}_int_novadrive"
          +materialized: view

    marts:
          +schema: "{{ env_var('DBT_ENV') }}_mart_novadrive"
          +materialized: table